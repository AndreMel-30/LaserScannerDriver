# LaserScannerDriver
A university project that started with the basic idea is to implement a driver in C++ that handles reads as a fixed-size circular queue (FIFO). It is a classic pattern in real-time sensor drivers.
